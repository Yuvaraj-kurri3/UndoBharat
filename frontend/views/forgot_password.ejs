<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password | UndoBharath</title>
    <link rel="icon" type="image/png" href="https://res.cloudinary.com/dqz2hem3m/image/upload/v1750665330/logo_ep4az4.png">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        body { background: #f3f4f6; font-family: 'Poppins', sans-serif; }
        .container { max-width: 400px; margin: 60px auto; background: #fff; border-radius: 12px; box-shadow: 0 4px 24px rgba(0,0,0,0.08); padding: 32px 24px; }
        h2 { text-align: center; color: #333; margin-bottom: 18px; }
        .input-group { margin-bottom: 18px; }
        label { display: block; margin-bottom: 6px; color: #555; font-weight: 500; }
        input { width: 100%; padding: 12px; border: 1px solid #d1d5db; border-radius: 6px; font-size: 15px; }
        button { width: 100%; background: #667eea; color: #fff; border: none; border-radius: 6px; padding: 12px; font-size: 16px; font-weight: 600; cursor: pointer; margin-top: 8px; }
        button:disabled { background: #a5b4fc; }
        .msg { text-align: center; margin-bottom: 12px; color: #eab308; }
        .success { color: #16a34a; }
        .error { color: #dc2626; }
    </style>
</head>
<body>
    <div class="container">
        <h2>Forgot Password</h2>
        <% if (msg) { %>
            <div class="msg <%= msgType %>"><%= msg %></div>
        <% } %>
        <form id="emailForm" action="/forgot-password" method="post" <% if (step !== 'email') { %>style="display:none"<% } %>>
            <div class="input-group">
                <label for="email">Registered Email</label>
                <input type="email" id="email" name="email" required placeholder="Enter your email">
            </div>
            <button type="submit">Send OTP</button>
        </form>
        <form id="otpForm" action="/verify-otp" method="post" <% if (step !== 'otp') { %>style="display:none"<% } %>>
            <div class="input-group" style="position: relative;">
                <label for="otp">Enter OTP</label>
                <input type="text" id="otp" name="otp" maxlength="4" required pattern="\d{4}" placeholder="4-digit OTP">
                  <button type="button" id="togglePassword" style="position:absolute; right:12px; top:38px; background:none; border:none; cursor:pointer;">
            <svg id="eyeIcon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="#555" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-eye">
                <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
                <circle cx="12" cy="12" r="3"></circle>
            </svg>
        </button>
            </div>
            <button type="submit">Verify OTP</button>
        </form>


       <form id="resetForm" action="/reset-password" method="post" <% if (step !== 'reset') { %>style="display:none"<% } %>>
    <div class="input-group" style="position:relative;">
        <label for="password">New Password</label>
        <input type="password" id="password" name="password" required placeholder="Enter new password">
        <button type="button" id="togglePassword" style="position:absolute; right:12px; top:38px; background:none; border:none; cursor:pointer;">
            <svg id="eyeIcon" xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="#555" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-eye">
                <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
                <circle cx="12" cy="12" r="3"></circle>
            </svg>
        </button>
    </div>
            <button type="submit">Change Password</button>
        </form>
    </div>
</body>
</html>
